# SPDX-License-Identifier: LGPL-2.1-or-later

[Match]
Distribution=|centos
Distribution=|fedora

[Build]
Environment=
        GIT_URL=https://gitlab.com/redhat/centos-stream/rpms/systemd/
        GIT_BRANCH=c10s
        GIT_COMMIT=c10s
        PKG_SUBDIR=fedora

[Content]
Packages=
        clang-devel
        compiler-rt
        gdb
        git-core
        libasan
        libubsan
        rpm-build
        which
